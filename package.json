{
  "name": "polish-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "local": "npx ts-node server.ts",
    "vercel:dev": "vercel dev --listen 3001"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
